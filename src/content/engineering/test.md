---
title: Engineering log — pipeline test
---

This paragraph exists only to prove that the `/engineering` content pipeline
works: typed metadata in `src/data`, a Markdown body in `src/content`, and a
static page rendered by Astro.

## What this test article contains

A second paragraph under a small heading, so the renderer can be checked for
headings, links and prose styling before the real articles are written.
